# tests/fixtures/home/.thoth/memory/MEMORY.md — a git-TRACKED stand-in for `~/.thoth/memory/`.
# tests/cases/core.cyr (test_memory_root_identity) counts how many times the marker below reaches the memory
# system message: once when the walked-up `.thoth` is recognised as ~/.thoth, twice when it is not (0.45.2).
- FIXTURE-FACT-0453: a fact in the home store, injected once
