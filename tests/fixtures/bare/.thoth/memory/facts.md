# tests/fixtures/bare/.thoth/memory/facts.md — a git-TRACKED store with fact files but NO MEMORY.md.
# tests/cases/core.cyr (test_memory_index_witness) walks to it: with no index there is no byte witness, so the
# store is never called ~/.thoth on its account (the stated residual of the 0.45.5 closure).
- FIXTURE-FACT-BARE: a fact in a store without an index
